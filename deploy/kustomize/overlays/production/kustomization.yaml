apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: vibetown

resources:
  - ../../base
  - hpa.yaml
  - ingress.yaml

patches:
  - path: patches/deployment.yaml
  - path: patches/configmap.yaml
